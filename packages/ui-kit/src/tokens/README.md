# tokens/

The design token layer — color, typography, spacing, radius, shadow/glow,
and animation-speed tokens, per docs/02-design-system.md, Section 16 and
docs/05-frontend-architecture.md, Section 5. Not yet implemented; this is
where the Theme Engine's CSS-custom-property resolution logic (referenced
by both apps' theme/index.css files) will eventually be defined once.
