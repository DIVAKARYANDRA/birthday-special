# primitives/

Domain-agnostic UI primitives (Button, Card, GlassPanel, Modal, Tooltip,
Badge, ProgressBar) implementing docs/02-design-system.md, Section 4's
component language. Mirrors apps/web/src/components/ui/ — once this
package is implemented, that folder's contents are expected to move here
so both apps share one implementation rather than two parallel ones.
