# app/

**Status:** placeholder — foundation only, no implementation yet (Prompt 7).

**Purpose:** App shell: providers (Theme, React Query, Auth/Session, Audio) and router configuration. Currently holds only a minimal bootstrap (main.tsx, App.tsx) to validate the build pipeline.

**Reference:** docs/05-frontend-architecture.md, Section 1
