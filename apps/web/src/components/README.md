# components/

**Status:** placeholder — foundation only, no implementation yet (Prompt 7).

**Purpose:** Presentation-only, reusable components with zero domain knowledge. `ui/` holds the low-level design-system primitives (Button, Card, GlassPanel, Modal...); `global/` holds cross-cutting chrome mounted once at the app root (particles layer, music player, unlock toast).

**Reference:** docs/05-frontend-architecture.md, Section 4
