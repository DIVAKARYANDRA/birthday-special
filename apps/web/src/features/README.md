# features/

**Status:** placeholder — foundation only, no implementation yet (Prompt 7).

**Purpose:** Vertical-slice domain modules — each feature owns its own components, hooks, local state, and API calls together. Features are consumed by scenes, never the reverse.

**Reference:** docs/05-frontend-architecture.md, Section 1
