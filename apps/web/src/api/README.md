# api/

**Status:** placeholder — foundation only, no implementation yet (Prompt 7).

**Purpose:** Typed API client functions per backend domain, wrapped in React Query hooks. This is the ONLY layer permitted to make HTTP calls — feature code never calls fetch directly.

**Reference:** docs/05-frontend-architecture.md, Section 1/8
