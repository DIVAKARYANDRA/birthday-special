# assets/fonts/

Locally-bundled font files (if any font isn't loaded via the Theme Engine's
CSS-variable font-family references). Empty until a specific font file is
selected for a given typography role — see docs/02-design-system.md,
Section 3.
