# assets/icons/

A small set of always-needed icons only (e.g. UI chrome icons that must be
available before any content loads). Most iconography tied to specific
content (achievement badges, game tiles) is expected to be admin-managed
MediaAsset content instead, per docs/03-data-architecture.md, Section 3 —
this folder is reserved for genuinely static, critical-path icons only.
