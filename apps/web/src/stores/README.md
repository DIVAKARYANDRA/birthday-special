# stores/

**Status:** placeholder — foundation only, no implementation yet (Prompt 7).

**Purpose:** Zustand stores for client/interaction state only: useProgressionStore, useAudioStore, useThemeStore, useUIStore. Never a source of truth for anything the backend also tracks — server state lives in React Query, not here.

**Reference:** docs/05-frontend-architecture.md, Section 6
