# ci/

Continuous integration pipeline definitions (build, lint, test gates), per
docs/06-engineering-foundation.md, Section 8 (Git Workflow) and Section 9
(Testing Strategy).

## Status
Not yet configured. Deferred until there is real application code and a
test suite for a pipeline to meaningfully run against — configuring CI
against an empty foundation would provide no real verification value at
this stage.
